<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mykuis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyKuis">
        <activity android:name=".KalkulatorActivity"></activity>
        <activity android:name=".ConvertMoneyActivity" />
        <activity android:name=".HasilSkoringActivity" />
        <activity android:name=".KuisEssayActivity" />
        <activity android:name=".KuisPilihanGandaActivity" />
        <activity android:name=".KuisActivity" />
        <activity android:name=".SliderActivity" />
        <activity android:name=".OpsiActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>